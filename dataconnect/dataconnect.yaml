specVersion: "v1beta"
serviceId: "NexusQuest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "NexusQuest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
